import { Ngcolor3Directive } from './ngcolor3.directive';

describe('Ngcolor3Directive', () => {
  it('should create an instance', () => {
    const directive = new Ngcolor3Directive();
    expect(directive).toBeTruthy();
  });
});
