import { Ngcolor2Directive } from './ngcolor2.directive';

describe('Ngcolor2Directive', () => {
  it('should create an instance', () => {
    const directive = new Ngcolor2Directive();
    expect(directive).toBeTruthy();
  });
});
